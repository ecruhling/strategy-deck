import domReady from '@wordpress/dom-ready';

// domReady( function () {
// 	const deckCardCheckboxes = document.querySelectorAll(
// 		'.wp-block-strategydeck-deck-card input'
// 	);
//
// 	deckCardCheckboxes.forEach( ( deckCardCheckbox ) => {
// 		deckCardCheckbox.addEventListener( 'change', ( event ) => {
// 			if ( event.target.checked ) {
// 				// Checkbox is checked.
// 				console.log( 'Checkbox is checked..' );
// 			} else {
// 				// Checkbox is NOT checked.
// 				console.log( 'Checkbox is not checked..' );
// 			}
// 		} );
// 	} );
// } );
